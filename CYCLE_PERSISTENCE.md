# クール別勤務申請保存

この版では勤務申請ドラフトをPostgreSQLへクールごとに保存します。

- 管理者医師情報: `shift-admin-staff-v2`（全クール共通）
- 最後に開いたクール: `shift-current-cycle-v1`
- 勤務申請ドラフト: `shift-draft-YYYY-MM-DD`（クール開始日ごと）

動作:
1. 初めて開くクールは未設定状態。
2. 「初期化」で管理者医師名を反映し、勤務希望初期値を作成。
3. 入力内容はそのクール専用キーへ自動保存。
4. 別クールへ切り替える直前にも即時保存。
5. 保存済みクールへ戻ると、そのクールの勤務者名・希望・備考・管理者調整・必要人数等を復元。
6. ページ再読み込み時は最後に開いていたクールを復元。

旧版互換:
- 旧3コンテナ版の `shift-draft` が残っている場合、そのデータの `cycleStart` に一致するクールの初回読み込みに利用し、新しいクール別キーへ自動移行します。
